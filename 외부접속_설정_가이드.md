# 휴대폰에서 접속하기

두 가지 상황이 있습니다.

| 상황 | 필요한 것 | 준비 시간 |
|---|---|---|
| **집 와이파이 안에서만** | 아무것도 없음. 지금 바로 됨 | 0분 |
| **밖에서 (LTE·다른 와이파이)** | Streamlit Cloud + GitHub Gist | 약 30분 (한 번만) |

---

# A. 집 와이파이 안에서 (지금 바로)

1. PC 에서 `실행_윈도우.bat` 실행
2. 화면에 나오는 **같은 와이파이에서** 주소를 휴대폰 브라우저에 입력

```
http://192.168.x.x:8501
```

**접속이 안 되면 대부분 Windows 방화벽입니다.**
처음 실행할 때 뜨는 방화벽 창에서 **개인 네트워크**를 체크하고 허용하세요.
이미 놓쳤다면:
`제어판 → Windows Defender 방화벽 → 앱 또는 기능 허용 → python.exe 의 "개인" 체크`

> 이 IP 주소는 공유기가 자동으로 배정하는 것이라 PC 나 공유기를 재부팅하면
> 바뀔 수 있습니다. 매번 실행 화면에 표시되니 그때 확인하세요.

---

# B. 밖에서 접속하기 (Streamlit Cloud)

PC 를 켜두지 않아도 언제 어디서나 휴대폰으로 접속할 수 있습니다.
**전부 무료**이고, 한 번만 설정하면 됩니다.

준비물: GitHub 계정 하나

---

## 시작 전 — 저장소는 Public 으로 만듭니다

Streamlit Community Cloud 는 비공개(Private) 저장소도 배포할 수 있지만,
GitHub 권한 연동이 제대로 되지 않으면 `This repository does not exist` 오류가
계속 납니다. 권한 설정에서 막히면 **Public 으로 만드는 편이 빠릅니다.**

공개해도 보안상 실제 손해는 거의 없습니다.

| 항목 | 어디에 저장되나 | 공개되나 |
|---|---|---|
| 로그인 비밀번호 | Streamlit Secrets | 아니오 |
| GitHub 토큰 | Streamlit Secrets | 아니오 |
| 연봉·재산세 자료 | 비공개(secret) Gist | 아니오 |
| 앱 접속 | 비밀번호 로그인으로 차단 | 아니오 |
| 계산 로직 코드 | GitHub 저장소 | 예 |

공개되는 것은 계산 코드뿐입니다.

> **이미 Private 으로 만들어서 파일을 올렸다면**
> 그냥 Public 으로 바꾸지 말고 **저장소를 지우고 새로 만드세요.**
> Git 은 과거 커밋을 전부 보관하기 때문에, 파일을 나중에 고쳐도
> 첫 커밋에 들어간 내용은 히스토리에 남아서 공개됩니다.

---

## [1단계] 업로드용 폴더 만들기 (5분)

GitHub 웹페이지에 파일을 끌어다 놓는 방식은 `.gitignore` 를 **무시하고**
끌어놓은 것을 전부 올립니다. 비밀번호 파일과 개인 자료가 그대로 올라가면
안 되므로, 안전한 사본을 먼저 만듭니다.

앱 폴더에서 실행:
```
python 업로드_준비.py
```

옆에 `_업로드용` 폴더가 생깁니다. 이 폴더에는
`secrets.toml` 과 `data/` 가 빠져 있습니다. (실행하면 무엇을 뺐는지 화면에 나옵니다)

---

## [2단계] GitHub 에 코드 올리기 (10분)

### 2-1. 저장소 만들기
- [github.com](https://github.com) 로그인 → 우측 상단 **+** → **New repository**
- Repository name: `myprogram` (아무 이름이나)
- **Public** 선택
- Add README / .gitignore / license 는 전부 **Off**
- **Create repository**

### 2-2. 파일 올리기
1. 만들어진 화면의 파란 상자에서 **`uploading an existing file`** 클릭
2. 탐색기에서 `_업로드용` 폴더를 **엽니다** (폴더 자체를 끌면 안 됩니다)
3. 안의 내용물을 **전체 선택** (Ctrl+A) — 파일 13개 + 폴더 4개 = **17개 항목**
4. 브라우저의 점선 영역으로 끌어다 놓기
5. 업로드가 끝나면 아래로 스크롤 → 초록색 **Commit changes** 클릭
   ← 이걸 안 누르면 저장되지 않습니다

### 2-3. 확인
저장소 첫 화면에 `Home.py` 가 **맨 위 목록에** 있어야 합니다.
`pages/` 폴더를 눌러서 `1_🏦_대출_상환_계산기.py` 처럼 이모지·한글이
깨지지 않았는지도 확인하세요. 깨지면 메뉴가 뜨지 않습니다.

---

## [3단계] 토큰과 Gist 만들기 (5분)

메모장을 하나 열어두세요. 토큰은 **한 번만 보여주고 다시 볼 수 없습니다.**

### 3-1. 토큰 발급
1. GitHub → 우측 상단 프로필 → **Settings**
2. 왼쪽 메뉴 **맨 아래** → **Developer settings**
3. **Personal access tokens** → **Tokens (classic)**
4. **Generate new token** → **Generate new token (classic)**
   - **Note**: `dashboard` (아무 이름)
   - **Expiration**: `No expiration` (또는 원하는 기간)
   - **Select scopes**: **`gist` 하나만** 체크 ← 다른 건 건드리지 마세요
5. 맨 아래 **Generate token**
6. 나오는 `ghp_...` 를 **바로 메모장에 붙여넣기**

> `gist` 권한만 있는 토큰이라 유출돼도 저장소 코드나 다른 정보에는
> 접근할 수 없습니다.

### 3-2. 비공개 Gist 만들기
1. [gist.github.com](https://gist.github.com) 접속
2. **Filename**: `dashboard.txt`
3. 내용: 아무거나 (예: `hello`)
   → 이 파일은 자리만 채우는 빈 껍데기입니다. 토큰을 여기 넣으면 안 됩니다.
     앱이 나중에 이 Gist 안에 `salary_data.json` 같은 파일을 만들어 넣습니다.
4. 초록 버튼 옆 화살표를 눌러 **Create secret gist** 선택
   - 주의: `Create public gist` 가 아닙니다
5. 주소창 맨 뒤 문자열을 메모장에 복사

```
https://gist.github.com/내아이디/a1b2c3d4e5f6a7b8c9d0
                                 ~~~~~~~~~~~~~~~~~~~~ 이게 gist_id
```

---

## [4단계] Streamlit Cloud 에 올리기 (10분)

1. [share.streamlit.io](https://share.streamlit.io) → **GitHub 계정으로 로그인**
2. **Create app** → **Deploy a public app from GitHub** 의 **Deploy now**
3. 입력
   - **Repository**: `내아이디/myprogram`
   - **Branch**: `main`
   - **Main file path**: `Home.py`
     ← 기본값이 `streamlit_app.py` 이니 반드시 바꾸세요
4. **Deploy 누르기 전에** `Advanced settings` 를 클릭해
   **Secrets** 칸에 아래 세 줄을 붙여넣습니다

```toml
password = "본인이_정한_로그인_비밀번호"
github_token = "ghp_3단계에서_복사한_토큰"
gist_id = "3단계에서_복사한_gist_id"
```

5. **Deploy** → 2~3분 대기

완료되면 `https://....streamlit.app` 주소가 나옵니다.
휴대폰에서 열면 로그인 화면이 뜹니다.

---

## [4단계] 확인하기

앱에 로그인한 뒤 **왼쪽 사이드바 → 저장 위치** 를 펼쳐보세요.

| 표시 | 뜻 |
|---|---|
| **GitHub Gist** | 정상. 자료가 Gist 에 저장됩니다 |
| 경고 + **임시 저장** | Secrets 의 `github_token` / `gist_id` 가 빠졌습니다 |
| 경고 + **Gist 연결 문제** | 토큰이나 gist_id 가 틀렸습니다. 표시된 메시지를 확인하세요 |

정상 표시가 보이면, 아무 자료나 저장해 보고 [gist.github.com](https://gist.github.com)
내 Gist 에 `salary_data.json` 같은 파일이 생겼는지 확인하면 확실합니다.

---

## 기존 자료 옮기기

PC 에서 쓰던 자료를 클라우드로 가져오는 방법입니다.

1. **PC 에서** 앱 실행 → 각 페이지 사이드바 **백업 / 복원** → **백업 파일 내려받기**
   (연봉·재산세 각각)
2. 그 파일들을 휴대폰이나 PC 에서 접근 가능한 곳에 둡니다
3. **클라우드 앱에서** **자료 가져오기** 페이지 → 파일 올리기 → **가져오기 실행**

---

## 휴대폰에서 앱처럼 쓰기

접속한 화면에서
- **아이폰(Safari)**: 공유 버튼 → `홈 화면에 추가`
- **안드로이드(Chrome)**: 우측 상단 ⋮ → `홈 화면에 추가`

주소창 없이 전체화면으로 열려서 앱처럼 씁니다.

---

## 알아두면 좋은 것들

**PC 와 휴대폰을 번갈아 쓸 때**
두 기기 모두 같은 Gist 를 보므로 자료는 공유됩니다. 다만 앱이 자료를 5분간
기억하고 있어서, 한쪽에서 저장한 걸 다른 쪽에서 바로 보려면
사이드바 **저장 위치 → 다른 기기의 변경 내용 가져오기** 를 누르세요.

**PC 에서 쓸 때도 Gist 를 쓰고 싶다면**
`.streamlit/secrets.toml` 에 `github_token` 과 `gist_id` 두 줄을 넣으면 됩니다.
그러면 PC 와 클라우드가 같은 자료를 봅니다.

**Streamlit Cloud 앱이 잠들면**
한동안 아무도 접속하지 않으면 앱이 절전 상태가 됩니다.
다시 접속하면 깨어나는데 30초쯤 걸립니다. 자료는 Gist 에 있으니 안전합니다.

**코드를 수정했을 때**
GitHub 저장소의 파일을 바꾸면 Streamlit Cloud 가 자동으로 다시 배포합니다.
`data/` 는 저장소에 없으므로 자료에는 영향이 없습니다.

---

## 보안 점검 목록

- [ ] 업로드 전에 `업로드_준비.py` 를 실행했는가
- [ ] `secrets.toml` 을 저장소에 올리지 **않았는가**
- [ ] 토큰의 scope 가 `gist` **하나뿐**인가
- [ ] Gist 를 **secret** 으로 만들었는가 (public 아님)
- [ ] 비밀번호가 12자 이상이고 다른 사이트와 다른가
- [ ] (권장) 평문 `password` 대신 `password_sha256` 해시를 쓰는가
      ```
      python -c "import hashlib;print(hashlib.sha256(input('비밀번호: ').encode()).hexdigest())"
      ```

앱에 이미 적용된 보호장치: 비밀번호 상수시간 비교, 연속 10회 실패 시 5분 잠금,
12시간 후 자동 로그아웃.

---

# 참고: 쓰지 않기로 한 방법들

| 방법 | 왜 제외했나 |
|---|---|
| Cloudflare Tunnel | PC 를 계속 켜둬야 함 |
| Tailscale | PC 를 계속 켜둬야 하고 휴대폰에 앱 설치 필요 |
| 공유기 포트포워딩 | https 가 아니고 집 IP 가 노출됨. 권장하지 않음 |
| 비공개(Private) 저장소 | Streamlit Cloud 의 GitHub 권한 연동이 자주 막힘. 공개해도 코드만 공개되고 비밀번호·자료는 그대로 보호되므로 Public 이 실용적 |
| 구글 드라이브 / OneDrive 저장 | 개인 구글 계정에서는 서비스 계정이 파일을 만들 때 용량 오류(storageQuotaExceeded)가 나기 쉽고, OneDrive 는 Azure 앱 등록이 필요해 설정이 훨씬 복잡함 |

나중에 PC 를 항상 켜두게 되거나 상황이 바뀌면 다시 검토하면 됩니다.
